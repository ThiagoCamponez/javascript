/* Ele vai executar enquanto tiver posições no array ou enquanto tiver caracteres na string*/

let myName = 'Thiago';
let allNames = ['Thiago', 'João', 'Maria', 'Pedro', 'Ana', 'Maurício', 'Joaquim'];

for (let name of myName) {
    console.log(name);
}